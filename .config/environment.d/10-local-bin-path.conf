# ~/.local/bin holds the wallpaper/theming scripts, but it is not in systemd's
# default user PATH. Desktop entries and user services inherit THIS environment,
# not the shell's, so without this an "Exec=wallpaper-settings" launcher entry
# silently fails while the same command works fine in a terminal.
PATH=$HOME/.local/bin:$HOME/bin:$PATH
